# Documentation & Comment Refactor — Task Checklist

Execution checklist for [PLAN.md](PLAN.md). Phases are ordered so the repo is consistent after each one.
Findings referenced as A1-A10 (stale), B1-B10 (duplication), C1-C15 (comments) are defined in PLAN.md.

---

## Phase 0 — Baseline

- [x] Record starting counts: `wc -l docs/*.md */README.md */*/README.md *.md` — **13,490 lines / 22 files** in `docs/`, **2,753 lines / 17 files** in module READMEs
- [x] `./gradlew dokkaGeneratePublicationHtml` passes **before** any change — BUILD SUCCESSFUL
- [ ] `pip install mkdocs mkdocs-material mkdocs-github-admonitions-plugin Pygments && mkdocs build --strict` passes before any change — **does not pass**: 54 pre-existing warnings, almost all cross-directory links (e.g. `docs/architecture.md` → `../data/README.md`) that mkdocs' strict validator won't resolve outside `docs_dir`, plus a handful of dead anchors. Tracked as finding A11 in PLAN.md; fix belongs to Phase 1/2, not this baseline commit.
- [x] Note current alert count per file: `grep -rc "> \[!" docs/*.md` (baseline was guessed at ~45 across 13 files; **actual measured baseline is 88 across 18 files**)

## Phase 1 — Delete and re-nav

Do the deletions and the `mkdocs.yml` nav rewrite in **one commit** so the site never has dangling entries.

- [x] `git rm --cached docs/index.md` — already in `.gitignore:55` and overwritten by CI (A7)
- [x] Delete: `faq.md`, `tips.md`, `quick-reference.md`, `philosophy.md`, `data-flow.md`, `plugins.md`, `spotless.md`, `github.md`, `fastlane.md`, `dependency.md`, `performance.md`, `license.md`
- [x] Rewrite `mkdocs.yml` nav — 8 of the 10 target files exist today and are in nav (plus the still-live `dependency-injection.md`, absorbed into `data.md` in Phase 2); `data.md` and `build-and-tooling.md` get nav entries in Phase 2 when those files are created, per principle 5 ("no aspirational content" — no nav entry should point at a file that doesn't exist yet). Kept the API Reference entry; License now points at the GitHub blob URL (`https://github.com/atick-faisal/Jetpack-Android-Starter/blob/main/LICENSE`), same pattern already used in `README.md:113`.
- [x] Pin the plugin version in `.github/workflows/docs.yml` (currently installs unpinned; Beta-stage 0.1.1) — pinned to `==0.1.1`
- [ ] **Accept:** `mkdocs build --strict` passes — **does not fully pass yet**: 55 → 50 warnings after this phase's deletions. Remaining 50 are (a) dangling links *inside* surviving files (`guide.md`, `getting-started.md`, `troubleshooting.md`, `firebase.md`, `architecture.md`, `components.md`, `state-management.md`, `navigation.md`) that still reference now-deleted docs by name — fixed as each file is rewritten in Phase 2, not by deletion alone; (b) the cross-directory README link limitation from A11 (`../data/README.md` etc., unresolved by mkdocs' strict validator outside `docs_dir`); (c) a handful of dead in-page anchors, also A11. None of these are introduced by this phase's changes — full pass is a Phase 2/Phase 6 outcome.

> [!TIP]
> Salvage before deleting. Each deleted file has unique content that must land somewhere — work through
> Phase 2 with the deleted files still available in git history, not from memory.

## Phase 2 — Rewrite `docs/`

One item per target file. Each states its source material and its acceptance criterion.

- [x] **`navigation.md`** — from-scratch rewrite for Navigation 3 (A1). The single largest item; nothing in the current file is reusable. Cover `NavKey`, `Navigator`, `rememberNavigationState`, multi-back-stack `NavigationState`, `NavDisplay`, `ListDetailSceneStrategy`, and the assisted-inject pattern Nav3 forces for keyed args (`ItemViewModel`).
      **Accept:** zero occurrences of `NavController`/`NavHost`; every API named appears in `core/navigation/` or `app/src/main/kotlin/dev/atick/compose/ui/` — verified: 1,284 → 303 lines, 2 alerts, `mkdocs build --strict` warnings 50 → 46 (no regression; remaining warnings belong to unrewritten files)
- [x] **`architecture.md`** — absorb `philosophy.md` and `data-flow.md` diagrams; state the domain-layer rationale exactly once (B5); delete the false testing claim (A3)
      **Accept:** ≤ 400 lines; "domain layer" rationale appears once in `docs/` — verified: 601 → 261 lines; `grep -rni "domain layer" docs/` matches only `architecture.md` (3 lines, one section); false "testing planned" claim replaced with accurate `:core:testing`/Robolectric facts; `philosophy.md` had no diagrams or domain-layer content to salvage (corrects B5's file list — `data/README.md`'s "why no domain layer" and `data-flow.md`'s general diagrams were the real sources); pattern-specific `data-flow.md` diagrams (network-only/local-only/offline-first/caching) left in git history for the future `data.md` task; the 330-line "Integration Patterns" section (stale pre-Nav3 API in its Navigation+State example) cut to one trimmed end-to-end flow diagram; `mkdocs build --strict` warnings 46 → 41, no regressions
- [x] **`state-management.md`** — `UiState`, `StatefulComposable`, the three update extensions, context parameters, `OneTimeEvent`; fix the "Kotlin 2.0" attribution (A10)
      **Accept:** ≤ 350 lines; every snippet greppable in `core/ui/` — verified: 949 → 226 lines; "Kotlin 2.0" attribution replaced with the correct fact (Kotlin 2.4.10, context parameters are default since 2.4, no compiler flag needed — grounded in `build-logic/convention/.../KotlinAndroid.kt`'s own comment explaining the flag was dropped as redundant); every code snippet is a real excerpt from `ItemViewModel.kt`, `HomeViewModel.kt`, `ItemScreen.kt`, `StatefulComposable.kt`, or `OneTimeEvent.kt` (spot-checked via `grep`), replacing all invented Post/User/Dashboard examples; documented the previously-unstated reentrancy guard and error-reset behavior of the async update functions; cut the ungrounded "Advanced Patterns" and generic "Migration Guide" (LiveData/sealed-class) sections and the duplicate before/after example that repeated across two sections; `mkdocs build --strict` warnings 41 → 37, no regressions (one new outgoing warning is the same accepted `../core/ui/README.md` cross-directory limitation, A11, that `architecture.md` already carries)
- [x] **`guide.md`** — the one canonical feature walkthrough, built from real repo code (B2)
      **Accept:** ≤ 300 lines; the walkthrough exists nowhere else in the repo — verified: 430 → 279 lines; complete rewrite around the real `home`/`Jetpack` feature (the only feature touching every layer — Room, offline-first repository with Firebase sync, two ViewModels, two Navigation 3 destinations), replacing every invented `Feature`/`NetworkFeature`/`FeatureRepository` symbol from the old draft; every code block spot-checked against its source file (`Jetpack.kt`, `JetpackEntity.kt`, `LocalDataSourceImpl.kt`, `HomeRepository(Impl).kt`, `HomeViewModel.kt`, `ItemViewModel.kt`, `HomeScreen.kt`, `HomeNavigation.kt`, `RepositoryModule.kt`); dropped dead links to three already-deleted docs (`philosophy.md`, `data-flow.md`, `dependency.md`) and the false "testing infrastructure is upcoming" claim (A3), replaced with real pointers (`JetpackDaoTest.kt`, `MainDispatcherRule.kt`, the `core/testing` fakes); deep mechanics (state update semantics, Nav3 argument passing, full two-way sync) are linked to their canonical doc rather than restated, per Principle 1; `mkdocs build --strict` warnings 37 → 36, no regressions (guide.md's 3 outgoing warnings are the same accepted cross-directory README-link limitation, A11, that `architecture.md`/`state-management.md` already carry)
- [x] **`components.md`** — reference table over prose; add Material Expressive theming (A5) and the `:lint` `DesignSystem` check that enforces the wrappers (A8)
      **Accept:** ≤ 400 lines, down from 1,556; every component name resolves in `core/ui/components/` — verified: 1,556 → 244 lines; all 24 `Jetpack*`/component names spot-checked against `core/ui/src/main/kotlin/dev/atick/core/ui/components/*.kt` (grep confirms each `fun` signature, incl. `RowScope.JetpackNavigationBarItem`); replaced from-scratch prose (Purpose/Usage/Parameters/Features per item) with grouped reference tables and real usage snippets grepped from `SignInScreen.kt`, `HomeScreen.kt`, `JetpackApp.kt`, `SettingsDialog.kt`; deleted invented content — the Mermaid flowchart with fabricated node IDs (`JetpackTopAppBar1`, `MaterialFAB`), nonexistent `GoogleSignInButton`/`FacebookSignInButton`, and the generic Accessibility/Custom-Component-Creation/Component-Organization/Performance/XML-Migration sections tied to no real code; added `JetpackTheme`'s `MaterialExpressiveTheme` basis and dynamic-color fallback (A5, `core/ui/theme/Theme.kt:106-154`) and the `DesignSystemDetector` lint check (A8, `lint/.../DesignSystemDetector.kt`, `lintPublish` wiring in `core/ui/build.gradle.kts:78`) — previously zero doc mentions of either; noted honestly which components are real but not yet wired into any screen, rather than inventing a usage story; dropped the dead `quick-reference.md` link; `mkdocs build --strict` warnings 36 → 32 (this file's own dead links to already-deleted docs removed; no regressions)
- [x] **`data.md`** — new file: data sources, repository pattern, two-way sync, `suspendRunCatching` → `Result` (B7)
      **Accept:** ≤ 400 lines; **no mention of `networkBoundResource`** (A2) — verified: 286 lines, `grep -c networkBoundResource docs/data.md` → 0; every snippet is a real excerpt spot-checked against `JetpackEntity.kt`, `UserPreferencesDataSourceImpl.kt`, `FirebaseDataSourceImpl.kt`, `HomeRepository(Impl).kt`, `ProfileRepositoryImpl.kt`, `RepositoryModule.kt`, `CoroutineUtils.kt`, `Sync.kt`, `SyncWorker.kt`; also absorbed the still-live `dependency-injection.md` (1,046 lines, deleted) per `TASKS.md:21` — its `@Binds`/`@IoDispatcher`/`@AssistedInject` essentials now live inline in `data.md` where they're actually used, rather than as a standalone generic DI guide; corrected two fabrications found in the current `data/README.md` while researching (a `Resource<T>` wrapper and `SyncAction.CREATE`/`UPDATE` values that don't exist — real enum is `NONE, UPSERT, DELETE`), neither of which made it into the new doc; the deleted `data-flow.md`'s push/pull sequence diagram was corrected from parallel (`par`) to sequential to match the real code's push-before-pull ordering (`HomeRepositoryImpl.kt:178-179`); `mkdocs.yml` nav's `Dependency Injection` entry replaced with `Data: data.md` in the same slot; dangling links to `dependency-injection.md` fixed in the two already-rewritten canonical files that carried them (`architecture.md:163,258`, `guide.md:128,277`) — `troubleshooting.md`'s two links left alone, deferred to its own not-yet-started rewrite along with its other dangling links to deleted docs; `mkdocs build --strict` warnings held at 32 (no regression: `dependency-injection.md`'s removal and the two link fixes exactly offset the one new cross-directory warning from `data.md`'s own `../sync/README.md` link, A11)
- [x] **`build-and-tooling.md`** — new file absorbing 6 deleted docs; must cover the previously undocumented subsystems (A8): `:lint`, APK badging, Dependency Guard, Gradle Managed Devices, `compose_compiler_config.conf`, `.run/` configs, Spotless-via-init-script
      **Accept:** ≤ 500 lines; every Gradle task named is runnable — verified: 222 lines; content recovered from the deletion commit (`44fc924c5^`) for `plugins.md`, `dependency.md`, `spotless.md`, `github.md`, `fastlane.md`, and the build-relevant half of `performance.md`, but rewritten from the real current implementation rather than carried over — the old docs' Renovate/Dependabot section was fabricated (`renovate.json`/`.github/dependabot.yml` don't exist, `grep -c "Renovate\|Dependabot" docs/build-and-tooling.md` → 0), the CI workflow file/job names had all been renamed (`plugins.md`'s "Build"/"Documentation"/"Release" → real `ci.yml`/`cd.yml`/`docs.yml`/`claude.yml`), and the secret list was undercounted (4 → real 5, missing `GOOGLE_WEB_CLIENT_ID`); every named Gradle task spot-verified with `--dry-run`/`help --task` against the real project, including a caught-and-fixed error — Gradle Managed Devices are wired only into `:benchmarks` (`benchmarkRelease`/`nonMinifiedRelease` variants), so the doc's first draft (`pixel6Api33DebugAndroidTest`) didn't exist; corrected to `:benchmarks:pixel6Api33BenchmarkReleaseAndroidTest`, confirmed via dry-run; `.run/` count corrected to the real 13 (`PLAN.md` had guessed 12); added `mkdocs.yml` nav entry between `Data` and `Firebase`; `mkdocs build --strict` warnings 32 → 34 (both new warnings are the same accepted `../fastlane/README.md`/`../CONTRIBUTING.md` cross-directory limitation, A11, that `architecture.md`/`data.md`/`guide.md`/`state-management.md` already carry — no regression); 2 alerts (⚠️ WARNING for the Spotless init-script task-discoverability trap, ❗ IMPORTANT for the baseline-profile manual-regeneration contract), within the ≤2/page convention
- [x] **`getting-started.md`** — clone → run in 5 minutes; correct JDK to 21 only (A10)
      **Accept:** ≤ 200 lines; a fresh clone succeeds following only this file — verified: 174 → 76 lines; the file's own JDK guidance was already correct (JDK 21, stated twice) so A10's actual error (JDK 11/17 offered) is confined to `troubleshooting.md`, not fixed here — deferred to that file's own rewrite; fixed 7 dead outbound links left over from Phase 1 deletions (`github.md`, `spotless.md`, `philosophy.md`, `dependency.md`, `plugins.md`, `performance.md`, `tips.md`, `fastlane.md`), remapped to `build-and-tooling.md`/`architecture.md`/`guide.md`/`troubleshooting.md`; deleted the `## Common Issues` section (B3) that duplicated `troubleshooting.md`'s build/Firebase/release symptom lists, replaced with a single link; collapsed two near-duplicate NOTEs about the template `google-services.json` into one plain sentence, bringing the page from 4 alerts down to 2 (budget); added a ☠️ `CAUTION` for the previously-undocumented debug-signing fallback (C8, `app/build.gradle.kts:75-87` — line numbers spot-checked, still current) — the first `CAUTION` anywhere in `docs/` (`grep -n "CAUTION" docs/*.md` was empty before); kept the `## Release Build Setup` heading text verbatim so `troubleshooting.md:1713`'s `#release-build-setup` anchor link stays valid ahead of that file's own rewrite; `mkdocs build --strict` warnings 34 → 24, no regressions (all ten fewer warnings are this file's own now-removed dead links)
- [x] **`firebase.md`** — console setup, config files, security rules, Credential Manager specifics
      **Accept:** ≤ 250 lines — verified: 201 → 146 lines; added the previously-undocumented Credential Manager sign-in-vs-register contract (`setFilterByAuthorizedAccounts(true)` vs `(false)`, `AuthDataSourceImpl.kt:173-193`) and a Crashlytics section grounded in the real `CrashReporter`/`FirebaseCrashReporter.reportException()` abstraction — previously zero doc mentions of either; corrected the one code snippet using `FirebaseAuth.getInstance()` (an API style never used in the real Hilt-injected code) and fixed dead links to already-deleted `github.md`/`faq.md`; deleted the file's own `## Troubleshooting` section (it restated a subset of `troubleshooting.md`'s richer `## Firebase Issues`), replaced with a single link, matching the pattern `getting-started.md`'s rewrite already established; kept the `## Firestore Security Rules` heading verbatim since `firebase/firestore/README.md:72,79` links to `firebase.md#firestore-security-rules` and that module README isn't rewritten until Phase 3; verified the rules' path (`/dev.atick.jetpack/{userId}/jetpacks/{jetpackId}`) and 7-field `isValidJetpack` check against the real `FirebaseDataSourceImpl.kt` and `FirebaseJetpack.kt` — both were already correct, filled in the `// ... other validations` placeholder with the real fields; deliberately did not port `troubleshooting.md`'s "Firebase Analytics Not Tracking" entry — confirmed fictional, since `firebase:analytics` is Crashlytics-only (no `AnalyticsLogger.kt` exists) per that module's own README; `mkdocs build --strict` warnings held at 24 (no regression); 2 alerts (⚠️ WARNING for the `google-services.json` secrecy trap, 💡 TIP for the Credential Manager flag rationale), within the ≤2/page budget
- [x] **`troubleshooting.md`** — rebuild as `<details>` entries, one per symptom, absorbing all six scattered troubleshooting sections (B3); drop the false testing claim (A3)
      **Accept:** **≤ 250 lines**, down from 2,071; no other `docs/` file has a troubleshooting section — verified: 2,071 → 237 lines, 29 `<details>` entries across 10 categories (Build Errors, Runtime Errors, Firebase, Sync, Code Quality & CI, Build Configuration, Data Layer, Memory, Testing, Getting Additional Help); of B3's six originally-scattered sections, five were already absorbed/trimmed by prior Phase 2 rewrites (`navigation.md`, `firebase.md`, `getting-started.md`, `dependency-injection.md`/`fastlane.md` deleted) — only `sync/README.md`'s 231-line `## Troubleshooting` section (sync-not-running, retry failures, Android 13+/14+ notification permissions, WorkManager constraints, conflict resolution, ADB debugging) was still unabsorbed; its unique content now lives in a new `## Sync` category, and `sync/README.md` (331 → 115 lines) was trimmed to a single pointer link, matching the pattern already used for `firebase.md`/`getting-started.md` in earlier phases — the full 4-section README template rewrite for `sync/README.md` remains its own unchecked Phase 3 item; dropped the false "testing infrastructure … Upcoming 🚧" claim (A3, was `troubleshooting.md:2012`), replaced with real `./gradlew test`/`:core:testing`/`MainDispatcherRule` guidance linking to the Testing sections `guide.md`/`navigation.md`/`state-management.md` already carry; dropped the fictional "Firebase Analytics Not Tracking" entry (no event-logging API exists in `firebase:analytics`, confirmed Crashlytics-only); rewrote every pre-Navigation-3 entry (`popUpTo`, `NavGraphBuilder`, nested graphs, `toRoute()`, citing nonexistent `TopLevelNavigation.kt`/`JetpackNavHost.kt`) as one accurate Nav3 entry linking to `navigation.md`; fixed the wrong application class name (`JetpackApplication` → real `App`, `app/src/main/kotlin/dev/atick/compose/App.kt`) and the wrong claim that `Sync.initialize()` is called from `App.onCreate()` — it's actually called from `SyncManagerImpl.requestSync()` (`sync/src/main/kotlin/dev/atick/sync/manager/SyncManagerImpl.kt:178`), confirmed by reading both files; fixed the stale `-Xcontext-receivers` compiler-flag advice (context parameters are on by default in Kotlin 2.4.10, `KotlinAndroid.kt:82`) and the JDK 17-vs-11 confusion (project requires JDK 21); merged internal duplicates (three recomposition entries → one, three preview entries → one, two Firebase-init entries → one, Spotless + CI-spotlessCheck → one); every code/file/line reference spot-checked against the real repo (`SyncWorker.kt:202-205`'s actual default `SyncConstraints`, `App.kt`/`MainActivity.kt`'s real Hilt annotations, `CoilModule.kt`/`DynamicAsyncImage.kt` real paths, `CrashReporter.kt`/`FirebaseCrashReporter.kt` real Crashlytics abstraction — replacing a fabricated `mappingFileUploadEnabled` config block that doesn't exist in `app/build.gradle.kts`); preserved the inbound `getting-started.md#release-build-setup` anchor (unchanged, still the only anchor other files depend on); 2 alerts (☠️ CAUTION for the debug-signing fallback, 💡 TIP for ADB sync-debugging commands), within the ≤2/page budget; `mkdocs build --strict` warnings 24 → 11 (all eleven remaining are the pre-existing cross-directory README-link limitation, A11 — no new warnings, no regressions)

## Phase 3 — Module READMEs

Four sections each — *Purpose* → *Key APIs* (table) → *Gotchas* (only if real) → links. **≤ 80 lines.**

> [!IMPORTANT]
> Keep the leading `# Module :name` line verbatim — Dokka consumes it as module documentation.

Rewrites:

- [x] `data/README.md` (484 → ≤ 80) — remove `networkBoundResource` (A2), drop duplicated repository patterns (B7) — verified: 36 lines; `SyncManager`/`Syncable` cross-module split (declared in `data`, implemented in `sync`) called out as a gotcha; `SyncAction.CREATE/UPDATE` fixed to real `NONE, UPSERT, DELETE`
- [x] `sync/README.md` (331 → 115 → ≤ 80) — drop the 229-line troubleshooting section (B3, already done pre-Phase-3) — verified: 35 lines; removed the fabricated battery/storage/idle work constraints (real constraint is `NetworkType.CONNECTED` only, confirmed in `SyncWorker.kt`)
- [x] `core/preferences/README.md` (291 → ≤ 80) — verified: 37 lines; every symbol (`UserPreferencesDataSource`, `UserDataPreferences`) spot-checked against real source; noted the dead `DarkThemeConfigSerializer` (C14) as a real gotcha
- [x] `core/room/README.md` (285 → ≤ 80) — verified: 34 lines; invented `UserEntity`/`UserDao` replaced with real `JetpackEntity`/`JetpackDao`; `SyncAction.CREATE/UPDATE` fixed to `NONE, UPSERT, DELETE`
- [x] `core/ui/README.md` (238 → ≤ 80) — drop the duplicated Philosophy section (B5) — verified: 28 lines; reduced to a thin index linking to `state-management.md`/`components.md` (already the canonical homes per Principle 1) rather than restating either
- [x] `core/network/README.md` (205 → ≤ 80) — remove `networkBoundResource` (A2) — verified: 40 lines; invented `UserApi`/`UserNetworkDataSource` replaced with real `JetpackRestApi`/`NetworkDataSourceImpl`; fabricated `apiKey`/`apiEndpoint` secrets snippet replaced with the real `BACKEND_URL` (`secrets.defaults.properties` → `BuildConfig.BACKEND_URL`, spot-checked against `RetrofitModule.kt`); added the real `getCurrentState()` no-initial-emission gotcha (second-tier finding from PLAN.md)
- [x] `core/android/README.md` (152 → ≤ 80) — remove `networkBoundResource` incl. the full fake snippet at :67 (A2) — verified: 32 lines; this was a second, previously-unlisted site of the same A2 fabrication (`Resource`/`networkBoundResource` don't exist in any `.kt`)
- [x] `app/README.md` (150 → ≤ 80, real MainActivity walkthrough kept) — verified: 48 lines; fixed the stale version-scheme snippet (`minorUpdateVersion = 0` → real `3`, confirmed in `app/build.gradle.kts`); fixed dead `dependency-injection.md` link → `data.md`
- [x] `firebase/firestore/README.md`, `firebase/auth/README.md`, `firebase/analytics/README.md` — verified: 35/31/25 lines; already-current links carried forward unchanged
- [x] `feature/settings/README.md`, `feature/auth/README.md`, `feature/home/README.md`, `feature/profile/README.md` — verified: 31/28/33/26 lines; found and fixed a real drift missed by PLAN.md's audit — all four READMEs named `SignInRoute`/`SignUpRoute`/`HomeRoute`/`ItemRoute`/`ProfileRoute` composables, but the Nav3 migration dropped the `Route` wrapper entirely (confirmed zero `Route(` matches under `feature/*/ui/`, and `docs/guide.md`/`docs/navigation.md` already call these `HomeScreen`/`ItemScreen` directly from `entries`); every signature replaced with the real `internal fun XxxScreen(...)`

New (A9):

- [x] `core/navigation/README.md` — highest value; the Nav3 model — verified: 35 lines; `Navigator`/`NavigationState`/`rememberDefaultEntryDecorators` spot-checked against source; links to `docs/navigation.md` rather than restating it
- [x] `core/testing/README.md` — `MainDispatcherRule`, the fakes, and their real (opt-in, not automatic) wiring — verified: 37 lines; corrected the task brief's premise — `core:testing` is a manual `testImplementation` in exactly three modules (`data`, `core/ui`, `feature/settings`, confirmed by grep), not wired into every module; `AndroidTest.kt`'s generic Robolectric/Turbine/Truth stack is what's universal, and that distinction is already correctly stated in `docs/architecture.md#testing`
- [x] `lint/README.md` — the two checks and how `lintPublish` ships them to consumers — verified: 30 lines; `TestMethodNameDetector` (WARNING, flags `test`-prefixed `@Test` methods) added alongside the already-documented `DesignSystemDetector` — this module doesn't apply the Dokka plugin, so its README isn't Dokka input, kept for the same four-section consistency anyway
- [x] `benchmarks/README.md` — baseline profile generation and startup benchmark — verified: 35 lines; task names cross-checked against `docs/build-and-tooling.md`'s own dry-run-verified Gradle Managed Device findings (`:benchmarks:pixel6Api33BenchmarkReleaseAndroidTest`) rather than the slightly-wrong task name in `StartupBenchmark.kt`'s own KDoc comment

**Accept:** `./gradlew dokkaGeneratePublicationHtml` passes — verified, BUILD SUCCESSFUL after all three Phase 3 commits; `grep -rn "networkBoundResource" .` returns nothing — verified across all 19 module READMEs.

## Phase 4 — Root files

- [x] `README.md` — correct the version block to Kotlin 2.4.10 / AGP 9.3.1 / Gradle 9.6.1 / Java 21 (A6); trim the repeated divider images (B10) — verified: 113 → 102 lines; version block now states exact patch versions matching `gradle/libs.versions.toml` (previously truncated to "Kotlin 2.4"/"Gradle 9.6", no AGP mentioned at all); trimmed the repeated `rainbow.png` divider from 4 occurrences to 1; rewrote the Documentation section, which linked 8 already-deleted files (`docs/dependency.md`, `docs/philosophy.md`, `docs/plugins.md`, `docs/spotless.md`, `docs/performance.md`, `docs/tips.md`, `docs/github.md`, `docs/fastlane.md`) — replaced with the real 10-file `docs/` set, matching `mkdocs.yml` nav order; `mkdocs build --strict` unaffected (README.md is outside `docs_dir`), still 11 warnings, no regression
- [x] `AGENTS.md` (441 lines) — slim to agent-specific rules plus pointers into `docs/`; remove the duplicated feature tutorial (B2); fix the stale navigation guidance (A1) — verified: 441 → 300 lines; deleted the 7-step "Example: Adding a New Feature" tutorial (B2, duplicate of `docs/guide.md`) in favor of a one-line pointer; replaced the stale `NavController`/`NavHost`/`composable<ProfileRoute>` navigation example (A1) with a real Nav3 snippet grepped from `HomeNavigation.kt` (`NavKey`, `Navigator`, `EntryProviderScope`); trimmed the State Management, Data Flow, and Dependency Injection pattern sections from full invented code blocks to short real-code excerpts plus links to their canonical docs homes, per Principle 1; fixed the File Naming/Code Organization conventions, which claimed a `{Feature}Route` composable split and a separate `viewmodel/` directory — neither exists (Phase 3 already found the `Route` wrapper was dropped repo-wide; ViewModels are co-located with their screen under `ui/{screen}/`, confirmed via `find`); fixed the essential commands: `dokkaHtmlMultiModule` doesn't exist (real task is `dokkaGeneratePublicationHtml`, confirmed via `--all`), and `spotlessApply`/`spotlessCheck` need `--init-script gradle/init.gradle.kts` or the task isn't found (confirmed against `docs/build-and-tooling.md`'s own already-verified guidance); removed the false "Renovate & Dependabot configured for auto-updates" claim (no `renovate.json`/`dependabot.yml` exist in the repo, confirmed via `find`); removed the stale `-Xcontext-parameters` compiler-flag claim (on by default since Kotlin 2.4, confirmed in `build-logic/.../KotlinAndroid.kt:82`'s own comment); added a pointer to Dependency Guard/APK badging regeneration commands, previously entirely unmentioned
- [x] `CHANGELOG.md` — correct the AGP version (A6) and backfill entries for Navigation 3, custom lint, baseline profiles, test harness, Dependency Guard, APK badging — verified: populated the previously-empty `[Unreleased]` section; all six features landed 2026-07-25, one day after the `v1.3.0` tag (2026-07-24), confirmed via `git log` on each feature's introducing commit (`3198c1fe9`, `196e726f5`, `ff6b22f6c`, `979451cb7`/`8ba479f12`, `3f5720f27`) — none were previously in the changelog; added a "Changed" entry for the AGP 9.1.0 → 9.3.1 bump; left the existing `[1.3.0]` entry's "Migrate to Android Gradle Plugin 9.1.0" untouched since it was accurate at the time of that release — a changelog is a historical record, not a live version snapshot, so A6's "correct the AGP version" is satisfied by documenting the subsequent bump under Unreleased rather than rewriting history
- [x] `CONTRIBUTING.md` (18 lines) — add the five-entry vocabulary table, the code-marker rules, the prose-why style note, and the "never use `!!!` syntax" rule. Mostly new content. — verified: 18 → 46 lines; added the vocabulary table verbatim-consistent with `PLAN.md`'s (`NOTE`/`TIP`/`IMPORTANT`/`WARNING`/`CAUTION`, emoji ↔ alert parity), the one-marker-per-block code rule, the prose-why style pointer to `core/navigation/NavigationState.kt:48-50`, and a `⚠️ WARNING` alert (dogfooding the convention it documents) against native `!!!`/`???` syntax; also fixed the existing checklist's `spotlessApply` command to include the required `--init-script` flag, consistent with the same fix in `AGENTS.md`

**Accept:** every version string in the repo matches `gradle/libs.versions.toml` and `gradle-wrapper.properties` — verified: `grep -n "Kotlin 2\.\|AGP 9\.\|Gradle 9\.\|Java 21" README.md AGENTS.md` matches `2.4.10`/`9.3.1`/`9.6.1`/`21` exactly; `mkdocs build --strict` holds at 11 warnings (no regression, none of the four files are in `docs_dir`); no dangling links to any of the 12 Phase 1-deleted docs remain in any of the four files.

## Phase 5 — Code comments

Prose-why style. Most items carry **no** marker — the marker column is the exception, not the default.

| # | File | Must explain | Marker | Done |
|---|---|---|---|---|
| C1 | `firebase/auth/.../AuthDataSourceImpl.kt:50` | What `CredentialManagerMisuse` flags, why it is suppressed class-wide, when to remove it. Model it on `app/lint.xml:20-29`. | ⚠️ | [x] |
| C2 | `core/ui/.../StatefulComposable.kt:260-292, 362-382` | Context parameters as the `viewModelScope` mechanism; `if (value.loading) return` as a re-entrancy lock that drops concurrent actions; the `data` snapshot taken before `launch`; the synthesized exception at :278 | ❗ | [x] |
| C3 | `core/ui/.../StatefulComposable.kt:101-105` | `getContentIfNotHandled()` mutates during composition; the `LaunchedEffect` key is the callback, not the error | ⚠️ | [x] |
| C4 | `feature/home/.../HomeViewModel.kt:56-59` | `onStart` + `WhileSubscribed(5000)` re-subscription starts a new permanent collector; note it is the template's most-copied idiom | ⚠️ | [x] |
| C5 | `sync/.../Sync.kt:124` | Add the missing **safety argument**: dropped requests are harmless because the worker drains all pending local changes. Simultaneously **cut** the three verbose Style B blocks at `Sync.kt:55-58`, `Sync.kt:103`, `SyncManagerImpl.kt:140` down to one. | 💡 | [x] |
| C6 | `data/.../HomeRepositoryImpl.kt:60-65` | `requestSync()` at :63 is outside the `flow {}` at :65, so it fires at construction, not collection | 💡 | [x] |
| C7 | `gradle/init.gradle.kts` | Why Spotless is an init script and not a plugin; that this is why `./gradlew tasks` omits `spotlessApply`; what the header regexes do | 💡 | [x] |
| C8 | `app/build.gradle.kts:78-85` | Release silently falls back to the debug key; output is un-shippable and un-upgradable; the fallback exists so the template builds on a fresh clone | ☠️ | [x] |
| C9 | `app/proguard-rules.pro:3-4` | The `{ *; }` breadth is deliberate-and-blunt, wider than the linked bug requires; note the overlap with the two consumer-rules files | ⚠️ | [x] |
| C10 | `firebase/auth/consumer-rules.pro:1-4` | The `playservices` provider loads via `ServiceLoader`; R8 full mode strips it; symptom is a provider-not-found at runtime. Match the per-line style of `core/network/consumer-rules.pro`. | — | [x] |
| C11 | `firebase/auth/.../AuthDataSourceImpl.kt:175,190` | Why `setFilterByAuthorizedAccounts` differs between sign-in and register, and that `true` is what makes a separate register path necessary | 💡 | [x] |
| C12 | `feature/settings/.../SettingsViewModel.kt:90-93` | Every non-Arabic locale silently becomes English; coupled to `generateLocaleConfig` at `app/build.gradle.kts:100`; adding `values-fr` will not update this `when` | ⚠️ | [x] |
| C13 | `core/ui/theme/{Background,Gradient,Tint}.kt:18` | Replace the three content-free TODOs: name the `io.nlopez.compose.rules` ruleset and the one-line `.editorconfig` fix. Also give `StatefulComposable.kt:80` a rationale (it is a state wrapper, not a layout node). | — | [x] |
| C14 | `core/preferences/.../UserDataSerializer.kt:83-108` | State that it is an unreferenced example for adopters, or delete it; note the `valueOf` exception type escapes the `readFrom` catch | ℹ️ | [x] |
| C15 | `build-logic/.../AndroidCompose.kt:67` | The `flatMap` + `provider {}` idiom returns an *absent* provider, which is the whole point | 💡 | [x] |

Second tier, if time allows: `settings.gradle.kts:20-29`, `core/navigation/NavigationState.kt:78,89`,
`core/network/.../NetworkUtilsImpl.kt:43-93`, `core/ui/.../ActivityExtensions.kt:337-349`,
`sync/.../SyncWorker.kt:150-165, 202`. **Deferred** — not done in this pass, per this section's own
"if time allows" scoping; left for a future task.

**Accept:** marker count within budget; `./gradlew spotlessCheck --init-script gradle/init.gradle.kts --no-configuration-cache` passes.
— verified: all 15 primary sites (C1-C15) done; line numbers had drifted slightly from this
table's original citations (re-verified against the current tree before editing, e.g. C1's
`setFilterByAuthorizedAccounts` calls are at :175/:190 exactly as cited, C2's functions span
:268-300/:378-396 post-edit) — no site was missing or already-fixed. C5's three duplicate
`ExistingWorkPolicy.KEEP` explanations were trimmed to one full comment (at the `enqueueUniqueWork`
call in `Sync.kt`, now carrying the missing safety argument) plus two short KDoc pointers, rather
than three restatements of the same mechanism. `grep -c` for the five emoji markers across
`*.kt`+`*.kts` → 13 total (11 in `.kt`, 2 in `.kts`: `app/build.gradle.kts` ☠️, `gradle/init.gradle.kts`
💡) — within the repo-wide 25-35 budget, using less of it than the ceiling since the second-tier
sites are deferred. `./gradlew spotlessCheck --init-script gradle/init.gradle.kts
--no-configuration-cache` passes (one round of `spotlessApply` was needed first: ktlint's
standard rule requires a blank line before a comment block that follows a property declaration —
applied automatically, comment text unaffected). `./gradlew build` and `./gradlew test` both pass
— comment-only changes, no behavior touched.

## Phase 6 — Verify

- [x] `mkdocs build --strict` — catches dangling nav entries and broken internal links
      **Accept:** 0 warnings — verified: installed the pinned toolchain (`mkdocs mkdocs-material mkdocs-github-admonitions-plugin==0.1.1 Pygments`) into an isolated venv (not previously installed on this machine); first run surfaced exactly the 11 known A11 cross-directory warnings (`../core/ui/README.md`, `../fastlane/README.md`, `../CONTRIBUTING.md`, `../sync/README.md`, `../firebase/{auth,firestore,analytics}/README.md` ×3, plus repeats) — fixed by converting all 11 to absolute GitHub blob URLs (`https://github.com/atick-faisal/Jetpack-Android-Starter/blob/main/<path>`), the same pattern `mkdocs.yml:67` already used for the License nav entry; sites: `architecture.md`, `build-and-tooling.md` (×2), `components.md`, `data.md`, `firebase.md` (×3), `guide.md`, `state-management.md`. A 12th warning (`api/index.html` nav entry not found) only appears when `docs/api/` hasn't been generated yet — reproduced the real CI sequence (`./gradlew dokkaGeneratePublicationHtml` → move `build/dokka/html/*` into `docs/api/`, per `.github/workflows/docs.yml`) before the final check; no dead in-page anchors found among the 30 anchor links audited. This closes out A11 and the deferred accept criteria from Phase 1 and Phase 2.
- [x] `./gradlew dokkaGeneratePublicationHtml` — a malformed `# Module :x` header breaks the API site
      **Accept:** BUILD SUCCESSFUL — verified; all 19 rewritten module README headers parsed correctly. (Unrelated pre-existing Dokka warnings about unresolved `[SyncManager]`/`[SyncWorker]`/`[Sync]` KDoc cross-links in `sync/`'s own source comments — a source-code KDoc issue, not a markdown doc, out of this refactor's scope.)
- [x] `./gradlew spotlessCheck --init-script gradle/init.gradle.kts --no-configuration-cache` — license headers survived
      **Accept:** BUILD SUCCESSFUL, all tasks UP-TO-DATE — verified; Phase 5's comment-only edits didn't disturb any license header.
- [x] `./gradlew build` and `./gradlew test` — no comment edit disturbed code
      **Accept:** both BUILD SUCCESSFUL — verified (`build`: 1943 tasks, 53 executed/2 cached/1888 up-to-date, 1m25s; `test`: 594 tasks, all up-to-date).
- [x] `grep -rn "NavController\|NavHost\|networkBoundResource" docs/ *.md */README.md */*/README.md` → nothing
      **Accept:** only expected contrastive/historical hits — verified: `docs/troubleshooting.md:61` explains "this uses Nav3 ... not `NavController`/`NavHost`" (contrastive, correct); `PLAN.md`/`TASKS.md`/`AGENTS.md` cite the terms only inside the audit's own historical findings and this checklist's own command text. No content in any doc presents either API as real/current. `docs/api/` (Dokka's generated cross-reference of the actual Android Navigation library types used transitively) is gitignored and untracked (`.gitignore:56`, `git ls-files docs/api` → 0), so it's correctly excluded from this check.
- [x] `grep -rn "^!!! \|^??? " . --include='*.md'` → nothing (native Material syntax breaks GitHub rendering)
      **Accept:** 0 matches — verified, clean.
- [x] `grep -rhno "^> \[![A-Za-z]*\]" --include='*.md' . | sed 's/.*\[!//;s/\]//' | sort -u` → only `NOTE`/`TIP`/`IMPORTANT`/`WARNING`/`CAUTION` in ALL-CAPS
      *(an unrecognized type silently degrades to a plain blockquote; `--strict` does **not** catch it)*
      **Accept:** exactly those 5 types, no others — verified.
- [x] `grep -c "^> \[!" docs/*.md` → no page above ~2, per GitHub's guidance

> [!NOTE]
> Both greps anchor on `^> [!` deliberately. `PLAN.md` and `CONTRIBUTING.md` document the vocabulary
> and therefore contain `` `> [!WARNING]` `` inside inline code and table cells. The plugin correctly
> does not convert those, and an unanchored grep would flag them as violations.
      **Accept:** max is 2 (`data.md`, `build-and-tooling.md`, `navigation.md`, `firebase.md`, `getting-started.md`, `guide.md`, `troubleshooting.md`); `architecture.md`/`components.md`/`state-management.md` have 1 — verified, within budget.
- [x] `grep -rc "ℹ️\|💡\|❗\|⚠️\|☠️" --include='*.kt' . | grep -v ":0"` → 25-35 total
      **Accept:** 12 across `.kt`, 13 including `.kts` (`app/build.gradle.kts`, `gradle/init.gradle.kts`) — verified; under the 25-35 ceiling by design (Phase 5 deferred the second-tier sites), not a violation — the budget is a ceiling, not a floor.
- [x] Spot-check ≥10-line snippets in the new docs are greppable in the repo
      **Accept:** verified 4 snippets across the highest-risk/newest files — `data.md:84-105` (`HomeRepositoryImpl.getJetpacks`/`createOrUpdateJetpack`) and `data.md:193-215` (`SyncWorker` class/`doWork`) both match real signatures in their source files; `build-and-tooling.md:117-122`'s `dependencyGuard {}` snippet matched the real block but its line-number comment had drifted (`171-174` → real `177-179` after unrelated edits since Phase 2) — corrected; `troubleshooting.md`'s 29 `<details>` entries structurally verified via the rendering check below rather than resnipped individually, since Phase 2 already spot-checked each one.
- [x] `mkdocs serve` **and** GitHub rendering both checked — dual rendering is the whole point of the plugin. Prioritize `troubleshooting.md` `<details>` and any `IMPORTANT`/`CAUTION` page, whose icon mapping is plugin-specific.
      **Accept:** found and fixed a real defect — verified: `mkdocs serve` on `127.0.0.1:8734` returned HTTP 200 for `troubleshooting/` and `getting-started/`, but inspecting the built HTML showed **all markdown inside `troubleshooting.md`'s 29 `<details>` blocks was rendering as literal unprocessed text** — no bold, no code spans, no links, and critically the two GitHub alerts nested inside `<details>` (💡 TIP at line 159, ☠️ CAUTION at line 180) rendered as literal `!!! tip "Tip"`/`!!! danger "Caution"` strings instead of admonition boxes. Root cause: Python-Markdown treats raw HTML blocks as opaque by default and does not process nested markdown unless the `md_in_html` extension is enabled and the tag carries `markdown="1"`. This is invisible on GitHub.com (GitHub's own renderer always processes markdown inside `<details>`), which is exactly why dual rendering is the checklist's own stated reason for this step — the bug would have shipped silently to the docs site. Fixed by adding `md_in_html` to `mkdocs.yml`'s `markdown_extensions` and `markdown="1"` to all 29 `<details>` tags plus the 13 `<summary>` tags containing backtick code spans (`<summary>` isn't a block element `md_in_html` processes by default, so span-level content needed the attribute too) in `troubleshooting.md`. Re-verified post-fix: `<strong>`/`<code>`/`<a>` all render correctly inside `<details>` bodies, `class="admonition tip"` and `class="admonition danger"` (with title "Caution") both present, `mkdocs build --strict` still 0 warnings. Confirmed the icon mapping matches PLAN.md's documented table: `CAUTION` → `admonition danger` / title "Caution", `WARNING`/`IMPORTANT` → `admonition warning` (with `IMPORTANT` keeping its own "Important" title). The `markdown="1"` attribute is inert on GitHub.com (an unrecognized attribute GitHub's HTML sanitizer passes through harmlessly) — verified by the attribute causing no change to any other already-correct rendering path.
- [x] Final counts: `docs/` ≤ ~3,500 lines, every module README ≤ 80
      **Accept:** `docs/*.md` totals 2,278 lines (well under 3,500); every module README ≤ 48 lines (max: `app/README.md`, `core/network/README.md`) — verified, both comfortably within budget.

**Accept:** all nine checks above pass; one real defect found and fixed (`md_in_html` rendering gap) that
was not anticipated by the original plan. `docs/api/` and `site/` (both gitignored, Dokka/mkdocs build
output) were regenerated locally for this verification and removed afterward — `git status` confirms
only the intended `docs/*.md`+`mkdocs.yml` changes are staged.

---

## Follow-up fixes

Real defects found during the audit. Documented honestly in Phase 5 comments, then fixed separately so
the documentation refactor itself stayed free of behavioral changes.

All seven are done, one commit each. Each was verified by reverting the fix and confirming the
new tests fail — two test drafts passed against the buggy code on the first attempt and had to be
rewritten, which is recorded per item below.

- [x] `core/ui/.../SwipeToDismiss.kt:78-81` — `onDelete()` is called in the composition body, re-firing on every recomposition while state sits at `EndToStart`. Should be a `LaunchedEffect(dismissState.currentValue)`. — fixed: wrapped in `LaunchedEffect(dismissState.currentValue)` so it runs once per transition into the dismissed state, with a ⚠️ marker explaining why the composition body is wrong here. Covered by a new `core/ui/.../components/SwipeToDismissTest.kt` (3 tests, Robolectric + `createComposeRule`, gesture-driven via `performTouchInput { swipeLeft() }`). The first draft of the recomposition test **passed against the buggy code** and was therefore worthless — it read the recomposition counter inside the `content` lambda, which recomposes only that child scope and never re-enters `SwipeToDismiss`'s own body; moved the read up into the calling scope (how a `LazyColumn` row actually recomposes) and confirmed it then fails on the old code with `expected: 1 but was: 4` — one deletion per recomposition — and passes on the fix.
- [x] `core/network/.../NetworkUtilsImpl.kt:43-93` — `callbackFlow` never emits an initial value, so `isOffline` reports `false` at launch and the offline banner never shows. `ActivityExtensions.kt:337-349` seeds correctly; the asymmetry is unexplained. — fixed: seeds `trySend(currentNetworkState())` before registering the callback, reading `activeNetwork` → `getNetworkCapabilities` → `NET_CAPABILITY_INTERNET`, then `.distinctUntilChanged()` so the platform's usual `onAvailable` replay doesn't double-emit. Removes the asymmetry with `ActivityExtensions.isSystemInDarkTheme`, which already seeded this way. New `core/network/src/test/.../NetworkUtilsImplTest.kt` (3 tests, Robolectric `ShadowNetworkCapabilities` + Turbine) — the module had no test dir before; verified all three fail with the seed removed, so they genuinely cover the defect. Also corrected two now-false claims in `NetworkUtils.kt`'s interface KDoc (that it emits only changes, and that collectors should add their own `distinctUntilChanged`) and one that was already wrong before this change — it documented `NetworkState.LOSING` as an emitted state, but `onLosing` only logs and never sends. `core/network/README.md`'s first *Gotchas* bullet described the defect as intended behavior and was replaced (40 lines, still ≤ 80).
- [x] `sync/.../SyncWorker.kt:181-183` — KDoc claims 10s/20s/40s backoff, but no `setBackoffCriteria` is set and WorkManager's default `MIN_BACKOFF_MILLIS` is 30s. The doc is actively wrong. — fixed, documentation only, no behavior change. Verified against the real WorkManager 2.11.2 sources (unpacked `work-runtime-2.11.2-sources.jar` from the Gradle cache) rather than from memory, which corrected **this item's own phrasing**: `MIN_BACKOFF_MILLIS` is 10s and is only the clamp floor for a *custom* delay — the value actually used when none is set is `DEFAULT_BACKOFF_DELAY_MILLIS` (30s), with `BackoffPolicy.EXPONENTIAL` as the `WorkSpec` default. `WorkSpec.calculateNextRunTime` computes `scalb(backoffDelay, runAttemptCount - 1)` capped at `MAX_BACKOFF_MILLIS` (5h), so the real schedule is ~30s/60s/120s. KDoc now states that, notes no `setBackoffCriteria` is set, and points at `startUpSyncWork` for changing it. Checked the two other backoff mentions (`Sync.kt:85`, `SyncWorker.kt:60`) — both say only "exponential backoff" with no numbers, so both remain accurate.
- [x] `data/.../HomeRepositoryImpl.kt:96,141,151` — `System.currentTimeMillis()` used as a distributed sync cursor against `whereGreaterThan("lastUpdated", ...)`. Clock skew silently drops remote records. Correct pattern is `FieldValue.serverTimestamp()`. — fixed: `FirebaseJetpack` gained `@get:ServerTimestamp var serverUpdatedAt: Timestamp?`, always written as `null` so Firestore substitutes its own timestamp (`@ServerTimestamp` *is* the `FieldValue.serverTimestamp()` mechanism this entry names — confirmed in `CustomClassMapper.serialize`, unpacked from `firebase-firestore-26.5.0-sources.jar` in the Gradle cache rather than recalled: "Replace null ServerTimestamp-annotated fields with the sentinel"). The value is stored locally as `JetpackEntity.serverUpdatedAtNanos` and read back by the new `JetpackDao.getSyncCursor` (`MAX(serverUpdatedAtNanos)`), replacing `getLatestUpdateTimestamp` one-for-one; `pullJetpacks(userId, syncedAfterNanos)` now filters `whereGreaterThan("serverUpdatedAt", …)`. Decisions taken with the user rather than assumed: the cursor lives in Room (not a DataStore field — `MainActivityViewModel` wraps the whole `UserDataPreferences` in its `UiState`, so a cursor there would re-emit UI state on every sync), and Room 1 → 2 rides the existing `fallbackToDestructiveMigration(true)` rather than a hand-written migration.
      Three findings the planning notes below got wrong or missed, corrected while implementing:
      · The cursor is **nanoseconds**, not milliseconds. Firestore timestamps carry nanosecond precision, so a cursor truncated to millis sits below the newest document it just ingested and re-pulls that document on every sync forever. `serverUpdatedAtNanos()` keeps `seconds * 1e9 + nanoseconds`; a `Long` covers it to the year 2262.
      · `serverUpdatedAt` had to go in the **class body, not the primary constructor**. `:firebase:firestore` declares the SDK with `implementation`, not `api`, so `com.google.firebase.Timestamp` is not on `:data`'s compile classpath — and `:data` builds `FirebaseJetpack(...)` with default arguments (`Jetpack.kt:118,133`), which would have dragged the type onto it and broken the module boundary. `:data` reads the value through the `serverUpdatedAtNanos()` extension and never names `Timestamp`; its *tests* do, via a new `testImplementation(libs.firebase.firestore)`, which is why that line exists.
      · Each pushed row now returns once on the following pull, because a write does not report the timestamp the server gave it. The echo carries exactly what was pushed so it changes no content — and it is what records the row's `serverUpdatedAtNanos` and moves the cursor past this device's own writes. Documented in `HomeRepositoryImpl` and covered by a test, rather than suppressed.
      New `data/src/test/.../HomeRepositoryImplTest.kt` (7 tests, file-local fakes for `LocalDataSource`/`FirebaseDataSource` following `ItemViewModelTest.kt`'s pattern; the fake Firestore filters on the same field the real query does, so a cursor read from the wrong field shows up as records that never arrive). Verified by reverting `HomeRepositoryImpl` to the old `MAX(lastUpdated) WHERE deleted = 0` cursor: exactly the three discriminating tests failed (`ingests a remote record older than the newest local edit`, `pulls from the server cursor, not the local edit clock`, `advances the cursor to the newest server timestamp pulled`) and the other 11 passed, so the suite genuinely covers the defect rather than the surrounding code. `JetpackDaoTest`'s three `getLatestUpdateTimestamp` tests were replaced by five for `getSyncCursor`, one of which inverts an old assertion on purpose: the cursor now **counts** soft-deleted rows, because their server timestamp has still been consumed and excluding them would rewind it.
      Docs updated in the same commit: `docs/data.md` (a two-clock table replacing the old cursor paragraph, plus the query snippet and sequence diagram — still 2 alerts, 317 lines), `docs/firebase.md` security rules (7 → 8 fields, and `serverUpdatedAt == request.time`, which is what stops a client forging the ordering), `core/room/README.md` (39 lines — its *Gotchas* bullet described the defect as intended behaviour, the same shape of correction the `NetworkUtilsImpl` fix needed), `firebase/firestore/README.md` (36 lines), and a short `CHANGELOG.md` **Fixed** entry. A first draft carried a compatibility path for Firestore documents written before this change — an unfiltered pull when the cursor is `0`, since a range filter skips documents missing the field — plus the docs to explain it. Cut on review: the `home`/`Jetpack` feature is demonstration code an adopter deletes, so no such documents can exist, and the branch was a special case an adopter would have had to read and reason about for nothing. A zero cursor filtered against the epoch already returns every document in a fresh project.
      Verified: `./gradlew build`, `test`, `spotlessCheck --init-script gradle/init.gradle.kts --no-configuration-cache`, `dokkaGeneratePublicationHtml` (three *new* unresolved KDoc links appeared on the first pass and were fixed — cross-package `[JetpackDao.getSyncCursor]`, and two links to an extension/member from the wrong scope), `:app:dependencyGuard` and `:app:checkReleaseBadging` (a `testImplementation` cannot reach the release runtime classpath, confirmed rather than assumed), and `mkdocs build --strict` at **0 warnings**. Marker count 17 across `.kt`+`.kts`, still under the 25-35 ceiling.
- [x] `feature/home/.../ItemViewModel.kt:94-97` — `toDoubleOrNull() ?: return` freezes the text field while the user types `1.` or clears it. Keep the raw string in state. — fixed: `ItemScreenData.jetpackPrice` is now a `String` defaulting to `""`, `updatePrice` stores the raw text unconditionally, and parsing happens once at submit (`trim().toDoubleOrNull() ?: 0.0`). Found and fixed a **second half of the same bug** not called out in this entry: `ItemScreen.kt:163` rendered `value = price.toString()`, so even a successful parse wrote `"1.0"` back over the `"1"` being typed — the field fought the user even when input was valid. `ItemScreen`/`EditItemForm`/`ItemScreenPreview` now take `price: String`. New `feature/home/src/test/.../ItemViewModelTest.kt` (7 tests) with `testImplementation(projects.core.testing)` added to `feature/home/build.gradle.kts` and a local fake `HomeRepository`. The first draft asserted on `itemUiState.value`, which is the **`stateInDelayed` initial value when nothing collects** — those assertions passed regardless of ViewModel behavior; rewrote them to collect through Turbine, then confirmed 3 of the 7 fail against the old drop-the-keystroke behavior. Docs updated in the same commit per Principle 2: `docs/state-management.md:92-94` and the save snippet in both `state-management.md` and `guide.md:175`, plus a new ❗ IMPORTANT stating the rule (both files still at 2 alerts, within budget).
- [x] `core/preferences/.../UserDataSerializer.kt:83-108` — `DarkThemeConfigSerializer` is dead code whose `valueOf` throws the wrong exception type to be caught by `readFrom`. — fixed by deletion, taking Phase 5's C14 ℹ️ comment with it (documenting dead code keeps the liability; removing it is the actual fix). Confirmed safe first: zero references anywhere in the tree, and `UserDataSerializerTest.kt`'s ten tests all target `UserDataSerializer`, so nothing needed updating there. Dropped the six imports that became unused (`KSerializer`, `PrimitiveKind`, `PrimitiveSerialDescriptor`, `SerialDescriptor`, `Decoder`, `Encoder`) plus `DarkThemeConfigPreferences`. `core/preferences/README.md:29-31`'s *Gotchas* bullet described the dead object; rewrote it as the rule that actually still matters — `readFrom` catches only `SerializationException`, so any custom serializer added here must throw that or a corrupt file escapes the fallback. Marker count now 14 lines across `.kt`+`.kts`, still well under the 25-35 ceiling.
- [x] `app/build.gradle.kts:119-126` — `onVariants` block is a no-op that reads the first output's `versionName` and writes it back. Either delete it or mark it as the hook point for Issue #579. — took both options: deleted the block, kept the TODO/FIXME comment above it (which carries the AGP 9 custom-APK-naming context and `Tracking: GitHub Issue #579`) as the marker for where the hook goes, extended with one line recording that a no-op block previously sat there so nobody re-adds it as a placeholder. Verified inert rather than assumed: `./gradlew :app:checkReleaseBadging` passes and `git diff app/release-badging.txt` is empty, so the built APK's package/version/sdk badging is byte-identical; `./gradlew :app:dependencyGuard` also passes.
